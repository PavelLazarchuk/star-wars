import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
	head: {
		fontSize: 20,
		fontWeight: 600,
	},
	details: {
		fontSize: 16,
		paddingBottom: 5,
	},
}));

export default useStyles;
