import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
	root: {
		padding: '20px 0',
	},
	link: {
		fontSize: 20,
		color: '#fff',
		textDecoration: 'none',
	},
}));

export default useStyles;
